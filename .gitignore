# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Node / Vite (UI, added later)
node_modules/
dist/
.vite/

# Tauri build output (added later)
/src-tauri/target

# Local secrets / fixtures that must never be committed
tests/fixtures/*.private
*.key
.env
.env.local
.live_account.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Materialized in CI from APPLE_PROVISIONING_PROFILE_BASE64 (see release.yml); never commit
app/src-tauri/embedded.provisionprofile
